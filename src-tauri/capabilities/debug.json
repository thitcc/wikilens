{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "debug",
  "description": "Permissions for the visual debug window. The page only listens to debug:// and ask:// events — it invokes no commands and needs no HTTP/FS; strictly smaller than the capture capability.",
  "windows": ["debug"],
  "permissions": ["core:default", "core:event:default"]
}
