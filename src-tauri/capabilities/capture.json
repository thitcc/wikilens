{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "Permissions for the transparent region-capture overlay window. It only invokes WikiLens's own capture commands (finish/cancel) and needs no HTTP/FS. The overlay capability is untouched.",
  "windows": ["capture"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
